package com.example.kevgps1;

public class SendSMS {

	
	// random poss solution method found here: http://stackoverflow.com/questions/18437287/send-my-current-location-via-sms-android
	
/*	public void sendLocationSMS(String phoneNumber, Location currentLocation) {
	    SmsManager smsManager = SmsManager.getDefault();
	    StringBuffer smsBody = new StringBuffer();
	    smsBody.append("http://maps.google.com?q="); 
	    smsBody.append(currentLocation.getLatitude());
	    smsBody.append(",");
	    smsBody.append(currentLocation.getLongitude());
	    smsManager.sendTextMessage(phoneNumber, null, smsBody.toString(), null, null);
	}
	*/
	
	
}
